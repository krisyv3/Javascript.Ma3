JavaScript MA03
----------------
Level 2

1. In your own words, describe what JavaScript Code Style is.
        Javascript codestyle is how the individual person like to write their own code with personal taste.
        In javascript there are a set of "unwritten" rules, like include block "{}", even when javascript would have run just fine without.

2. What is a Regular Expression?
        A regular Expression (Regex) is a "pattern" we can create to check for specific values.

3. What does the .random() method do?
        Used with math.random() it returns a value between 0 and 1.

4. What does the .sort() method do?
        .sort() sort's an array alphabetically

5. What does the .reverse() method do?
        .reverse() will reverse the order of the values inside the array.

6. List two examples of bad JavaScript code.
        var InvId = "1111";
        function invGen() {
        }
            Not following the camelCase and naming conventions.

        1 == "1";
            the usage of non-strict instead of strict when checking across different data types.

7. What data types can a JSON object contain?
        Boolean
        Number
        string
        object
        null
        Array

8. What is minifying JavaScript Code?
        Basically cleaning up the leftovers in the code, Making variables shorter and more precise, and generally remove everything thats not necessary.

9. What is a global variable?
        A global variable is a value that's accessible trough the global scope, and should generally not be used.
